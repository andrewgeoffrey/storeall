# Prevent direct access to PHP files in database folder
<Files "*.php">
    Order allow,deny
    Deny from all
</Files>

# Allow access to SQL files
<FilesMatch "\.(sql|txt)$">
    Order allow,deny
    Allow from all
</FilesMatch>
