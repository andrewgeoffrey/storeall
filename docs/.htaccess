# Prevent direct access to PHP files in docs folder
<Files "*.php">
    Order allow,deny
    Deny from all
</Files>

# Allow access to markdown files
<FilesMatch "\.(md|txt)$">
    Order allow,deny
    Allow from all
</FilesMatch>
